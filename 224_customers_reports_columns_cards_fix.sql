-- 224_customers_reports_columns_cards_fix
-- UI-only: cleans Customers reports tables, removes contact/avg columns, moves value to last column, confirms cards use current filtered dataset.
select '224 customers reports columns cards fix: frontend only' as info;
