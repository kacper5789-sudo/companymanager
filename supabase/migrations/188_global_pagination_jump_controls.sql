-- CompanyManager 113 — global pagination jump controls
-- Informational migration only. No database schema changes.
-- Frontend adds numbered pagination, ellipsis and direct page jump for long tables.
select '188 global pagination jump controls: frontend only' as info;
