-- CompanyManager 128 — pagination duplicate counter cleanup
-- UI-only patch: removes duplicated legacy table counters after global pagination renders.
select '202_pagination_duplicate_counter_cleanup: no database changes required' as info;
