-- 236_global_pagination_jump_fix
-- UI-only patch: fixes global direct page jump, uses full rendered rows, and stops pre-slicing tables in key modules.
select '236_global_pagination_jump_fix: frontend only' as info;
