-- CompanyManager 114 — pagination cleanup and jump fix
-- UI-only patch: removes duplicated legacy pager labels, fixes direct page jump and keeps activity history pagination consistent.
select '189_pagination_cleanup_jump_fix: no database changes required' as info;
